spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://141.98.1.214:5432/dogukan
    username: postgres
    password: clr2861
  jpa:
    hibernate:
      ddl-auto: none
      show-sql: true
      open-in-view: false
      properties:
        javax:
          persistence:
            schema-generation:
              create-source: metadata
              scripts:
                action: create
                create-target: create.sql

